image: Ubuntu 

stack: jdk 11  

branches:
  only:
    - main  

build: off  

install:
  - java -jar /Selenium/artifacts/app-order.jar &

build_script:
  - chmod +x gradlew
  - ./gradlew test --info
